<div class= "row">
<div class="genre" style="font-size :20px;" >
      ジャンル
    </div>

    <table class="table table-bordered table-hover text-center ec-genre-list" >
      <tr>
        <td>
          <%= link_to "全商品を表示", products_path, class:"ec-genre-list-item" %>
        </td>
      </tr>

      <% genres.each do |genre| %>
         <tr>
          <td>
            <%= link_to genre.name, customers_genre_path(genre.id), class:"ec-genre-list-item"%>
            </td>
        </tr>
           <% end %>

      </table>
</div>